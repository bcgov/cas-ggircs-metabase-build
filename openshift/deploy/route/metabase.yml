apiVersion: template.openshift.io/v1
kind: Template
labels:
  template: deploy-template

parameters:
- description: Openshift project name.
  displayName: Project
  name: OC_PROJECT
  required: true
- description: Prefix to prepend to object name.
  displayName: Prefix
  name: PREFIX
  required: true

objects:
- apiVersion: route.openshift.io/v1
  kind: Route
  metadata:
    name: ${PREFIX}metabase
  spec:
    host: metabase-${OC_PROJECT}.pathfinder.gov.bc.ca
    port:
      targetPort: ${PREFIX}metabase
    tls:
      termination: edge
    to:
      kind: Service
      name: ${PREFIX}metabase
      weight: 100
    wildcardPolicy: None
  status:
    ingress:
    - conditions:
      - status: 'True'
        type: Admitted
      host: metabase-${OC_PROJECT}.pathfinder.gov.bc.ca
      routerName: router
      wildcardPolicy: None


